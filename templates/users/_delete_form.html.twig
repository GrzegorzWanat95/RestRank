<div class="w-25 d-xxl-block d-xl-block d-lg-block d-md-block d-sm-none d-none">
    <form method="post" action="{{ path('app_user_delete', {'id': user.id}) }}" onsubmit="return confirm('Czy jesteś pewien, że chcesz usunąć konto? Tej operacji nie da się odwrócić');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
        <button id="delete__button" class="login__buttons">Usuń konto</button>
    </form>
</div>

<div class="w-100 d-xxl-none d-xl-none d-lg-none d-md-none d-sm-block d-block">
    <form method="post" action="{{ path('app_user_delete', {'id': user.id}) }}" onsubmit="return confirm('Czy jesteś pewien, że chcesz usunąć konto? Tej operacji nie da się odwrócić');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
        <button id="delete__button" class="login__buttons">Usuń konto</button>
    </form>
</div>


