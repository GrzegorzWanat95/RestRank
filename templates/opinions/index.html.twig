{% extends 'base.html.twig' %}

{% block title %}Opinions index{% endblock %}

{% block body %}
    <h1>Opinions index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>UserLogin</th>
                <th>Text</th>
                <th>RestaurantId</th>
                <th>Stars</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for opinion in opinions %}
            <tr>
                <td>{{ opinion.id }}</td>
                <td>{{ opinion.UserLogin }}</td>
                <td>{{ opinion.Text }}</td>
                <td>{{ opinion.RestaurantId }}</td>
                <td>{{ opinion.Stars }}</td>
                <td>
                    <a href="{{ path('app_opinions_show', {'id': opinion.id}) }}">show</a>
                    <a href="{{ path('app_opinions_edit', {'id': opinion.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_opinions_new') }}">Create new</a>
{% endblock %}
