{% extends 'base.html.twig' %}

{% block title %}Restauracja{% endblock %}

{% block body %}
    <h1>Restauracja</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ restaurant.id }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ restaurant.Name }}</td>
            </tr>
            <tr>
                <th>Miasto</th>
                <td>{{ restaurant.City }}</td>
            </tr>
            <tr>
                <th>Ulica</th>
                <td>{{ restaurant.Street }}</td>
            </tr>
            <tr>
                <th>Numer budynku</th>
                <td>{{ restaurant.AddressNumber }}</td>
            </tr>
            <tr>
                <th>Opis</th>
                <td>{{ restaurant.Description }}</td>
            </tr>
            <tr>
                <th>Typ</th>
                <td>{{ restaurant.Type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_restaurant_index') }}">Powrót do listy restauracji</a>

    <a href="{{ path('app_restaurant_edit', {'id': restaurant.id}) }}">Edytuj</a>

    {{ include('restaurant/_delete_form.html.twig') }}
{% endblock %}
