{% extends 'base.html.twig' %}

{% block title %}RestRank{% endblock %}

{% block body %}
            <div class="logged__form d-block pb-4">
                <div class="logged__panel flex-column d-flex d-xxl-block d-xl-block d-lg-block d-md-block d-sm-none d-none w-100">
                    <div class="d-flex justify-content-center align-content-center"><h1 class="register__header">Dodawanie restauracji</h1></div>
                </div>
            </div>

    {{ include('restaurant/_form.html.twig') }}

    <a href="{{ path('app_restaurant_index') }}">Powrót do listy restauracji</a>
{% endblock %}
