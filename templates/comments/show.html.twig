{% extends 'base.html.twig' %}

{% block title %}Comments{% endblock %}

{% block body %}
    <h1>Comments</h1>

    <table class="table">
        <tbody>
            <tr>

            </tr>
            <tr>
                <th>Text</th>
                <td>{{ comment.Text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_comments_index') }}">Powrót do listy opinii</a>

    <a href="{{ path('app_comments_edit', {'id': comment.id}) }}">Edytuj</a>

    {{ include('comments/_delete_form.html.twig') }}
{% endblock %}
